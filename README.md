# JuicyJS
JuicyJS reads every JS file in a folder you point it at, runs 44+ detection patterns against them, and gives you color-coded findings with exact line numbers, surrounding code context, and an actionable impact description for each finding.  No setup. No dependencies. Just Python 3.
